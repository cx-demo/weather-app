# weather-app

```
# Prompt

Build a weather app with city search. Show Singapore by default on load. Display current temperature in Celsius and a 5-day forecast in cards. Use weather-appropriate accent colors. Fetch live-data from Open-Meteo API (no key required) with hardcoded fallback if it fails. Center in a narrow column. Single index.html, no server/dependencies. Polished, sans-serif, 14-16px body, prefers-color-scheme
```
